# The 66MB binary is published via GitHub Releases, not committed.
TheQuestionGame.exe

# Python build/runtime artifacts
__pycache__/
*.py[cod]

# Mac build artifacts (py2app + hdiutil, produced by build-mac.yml)
dist/
build/
*.dmg
*.icns
icon.iconset/
