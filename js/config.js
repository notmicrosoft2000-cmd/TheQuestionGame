// AI CONFIG — THE TRANSMISSION
// Paste your free Groq API key below (https://console.groq.com/keys).
// WARNING: this file is served by GitHub Pages as-is, so the key is visible
// in the page source. Use a low-stakes key and rotate it if it gets abused.
// While the placeholder is left in place, the transmission falls back to
// scripted replies and the site still works.
window.AI_CONFIG = {
  groqKey: "PASTE_YOUR_GROQ_KEY_HERE",
  model: "llama-3.3-70b-versatile"
};
